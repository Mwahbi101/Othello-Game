#define VIDE ' '
char grille(char board[8][8])
{
    for (int i=0; i<8; i++)
        for (int j=0; j<8; j++)
           cout<<"entre la note"<<i+1<<endl;
            cin>>board[i][j];
        }
    }
}
